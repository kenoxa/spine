{
  "//": "run-queue review-stage settings overlay template. Rendered with absolute paths at review-stage spawn; passed via `claude -p --settings`. Write(*) and Edit(*) are NOT denied — /run-review Phase 4 must write review-findings.md and review-verdict.json into .scratch/; the guard hook (guard-queue-shell.sh) already blocks destructive patterns and out-of-repo writes.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "@@GUARD_PATH@@"
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": [
      "NotebookEdit(*)"
    ]
  }
}
